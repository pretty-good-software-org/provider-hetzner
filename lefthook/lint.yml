pre-commit:
  parallel: true
  commands:
    actionlint:
      glob: ".github/workflows/*.{yaml,yml}"
      run: actionlint -color -oneline {staged_files}
      fail_text: "actionlint found issues"

    yamllint:
      glob: "**/*.{yaml,yml}"
      run: yamllint --strict {staged_files}
      fail_text: "yamllint found issues"

    markdown-format:
      glob: "**/*.md"
      run: mdformat --check {staged_files}
      fail_text: "mdformat found formatting issues"

    markdownlint:
      glob: "**/*.md"
      run: markdownlint-cli2 {staged_files}
      fail_text: "markdownlint found issues"
